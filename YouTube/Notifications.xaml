<Page
    x:Class="YouTube.Notifications"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:YouTube"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource AppBackgroundBrush}">

    <Page.Resources>
        <Style x:Key="FlatButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Background="{ThemeResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="64"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="52"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="{ThemeResource AppBackgroundBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Uid="Loc_Notifications_Text"
                       Foreground="{ThemeResource AppPrimaryTextBrush}"
                       FontSize="26"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"
                       Margin="24,0,12,0"/>

            <Button Grid.Column="1"
                    Style="{StaticResource FlatButtonStyle}"
                    Width="64"
                    Height="64"
                    Click="SearchButton_Click">
                <Image local:ThemeAsset.Path="Assets/search.png"
                       Width="28"
                       Height="28"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
            </Button>
        </Grid>

        <ScrollViewer x:Name="MainScrollViewer"
                      Grid.Row="1"
                      VerticalScrollMode="Auto"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollMode="Disabled"
                      HorizontalScrollBarVisibility="Disabled"
                      Visibility="Collapsed">
            <StackPanel x:Name="NotificationsPanel"
                        Padding="0,8,0,16"/>
        </ScrollViewer>

        <StackPanel x:Name="EmptyPanel"
                    Grid.Row="1"
                    Visibility="Collapsed"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Margin="24">
            <TextBlock x:Uid="Loc_NoNotifications_Text" x:Name="EmptyTitleText"
                       Foreground="{ThemeResource AppPrimaryTextBrush}"
                       FontSize="20"
                       FontWeight="SemiBold"
                       TextAlignment="Center"/>
            <TextBlock x:Uid="Loc_NewUpdatesFromYourSubscriptionsWillAppearHere_Text" x:Name="EmptySubtitleText"
                       Foreground="{ThemeResource AppSecondaryTextBrush}"
                       FontSize="14"
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       Margin="0,8,0,0"/>
        </StackPanel>

        <Grid x:Name="LoadingGrid"
              Grid.Row="1"
              Visibility="Collapsed">
            <local:AndroidLoadingRing x:Name="LoadingRing"
                                      IsActive="False"
                                      Width="42"
                                      Height="42"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
        </Grid>

        <local:Tabbar x:Name="tabbar" Grid.Row="2"/>
    </Grid>
</Page>
