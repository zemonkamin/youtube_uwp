<Page
    x:Class="YouTube.Tabbar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:YouTube"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="#0F0F0F">
        <Grid.RowDefinitions>
            <RowDefinition Height="2"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <!-- Top dark gray bar -->
        <Border Grid.Row="0" Background="#FF222222"/>
        <!-- Bottom TabBar -->
        <Grid Grid.Row="1" Background="#0F0F0F">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Home -->
            <Button x:Name="HomeButton" Grid.Column="0" 
                    Background="Transparent" 
                    BorderThickness="0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Click="HomeTab_Click">
                <StackPanel Orientation="Vertical" 
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center">
                    <Image Source="Assets/tabbar/home-icon.png" 
                           Width="24" 
                           Height="24"
                           Stretch="Uniform"/>
                    <TextBlock Text="Home" 
                               Foreground="White" 
                               Margin="0,0,0,0"
                               FontSize="12" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>

            <!-- Shorts -->
            <Button x:Name="ShortsButton" Grid.Column="1" 
                    Background="Transparent" 
                    BorderThickness="0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Click="ShortsTab_Click">
                <StackPanel Orientation="Vertical"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center">
                    <Image Source="Assets/tabbar/shorts-icon.png" 
                           Width="24" 
                           Height="24"
                           Stretch="Uniform"/>
                    <TextBlock Text="Shorts" 
                               Foreground="White" 
                                Margin="0,0,0,0"
                              FontSize="12" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>

            <!-- Subscriptions -->
            <Button x:Name="SubscriptionsButton" Grid.Column="2" 
                    Background="Transparent" 
                    BorderThickness="0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Click="SubscriptionsTab_Click">
                <StackPanel Orientation="Vertical"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center">
                    <Image Source="Assets/tabbar/sub-icon.png" 
                           Width="24" 
                           Height="24"
                           Stretch="Uniform"/>
                    <TextBlock Text="Subscriptions" 
                               Foreground="White" 
                                Margin="0,0,0,0"
                              FontSize="12" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>

            <!-- Account -->
            <Button x:Name="AccountButton" Grid.Column="3" 
                    Background="Transparent" 
                    BorderThickness="0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Click="AccountButton_Click">
                <StackPanel Orientation="Vertical"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center">
                    <Image Source="Assets/tabbar/user-icon.png" 
                           Width="24" 
                           Height="24"
                           Stretch="Uniform"/>
                    <TextBlock Text="You" 
                               Foreground="White" 
                               Margin="0,0,0,0"
                               FontSize="12" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</Page>