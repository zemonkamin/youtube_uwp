<Page
    x:Class="YouTube.Login"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:YouTube"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource AppBackgroundBrush}">

    <Page.Resources>
        <SolidColorBrush x:Key="RefreshButtonBorderBrush" Color="{ThemeResource AppDividerColor}"/>

        <Style x:Key="PillOutlineButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{ThemeResource AppBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{ThemeResource AppPrimaryTextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource RefreshButtonBorderBrush}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="18.67,0"/>
            <Setter Property="FontSize" Value="13.33"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="MinWidth" Value="173.33"/>
            <Setter Property="Height" Value="54"/>
            <Setter Property="UseSystemFocusVisuals" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="PointerOver">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="RootBorder"
                                                             Storyboard.TargetProperty="Opacity"
                                                             To="0.92"
                                                             Duration="0:0:0"/>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="RootBorder"
                                                             Storyboard.TargetProperty="Opacity"
                                                             To="0.80"
                                                             Duration="0:0:0"/>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="RootBorder"
                                                             Storyboard.TargetProperty="Opacity"
                                                             To="0.45"
                                                             Duration="0:0:0"/>
                                            <DoubleAnimation Storyboard.TargetName="ButtonContentPresenter"
                                                             Storyboard.TargetProperty="Opacity"
                                                             To="0.45"
                                                             Duration="0:0:0"/>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>

                            <Border x:Name="RootBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="32">
                                <ContentPresenter x:Name="ButtonContentPresenter"
                                                  Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  Margin="{TemplateBinding Padding}"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Background="{ThemeResource AppBackgroundBrush}" x:Name="mainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="56"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="52"/>
        </Grid.RowDefinitions>

        <!-- Navbar at the top -->
        <local:Navbar x:Name="navbar" Grid.Row="0"/>

        <!-- QR Code section in the middle -->
        <StackPanel x:Name="qrContainer"
                    Grid.Row="1"
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    Margin="20,0,20,0">

            <TextBlock x:Uid="Loc_ScanTheQRCodeToSignIn_Text" x:Name="StatusText"
                       FontSize="18"
                       Foreground="{ThemeResource AppPrimaryTextBrush}"
                       HorizontalAlignment="Center"
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       Margin="0,0,0,20"/>

            <Border Background="White"
                    CornerRadius="5.33"
                    Padding="6.67"
                    MaxWidth="200"
                    MaxHeight="200">
                <Image x:Name="QrImage"
                       Stretch="Uniform"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
            </Border>

            <TextBlock x:Name="UserCodeText"
                       Text=""
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="{ThemeResource AppPrimaryTextBrush}"
                       HorizontalAlignment="Center"
                       TextAlignment="Center"
                       Margin="0,20,0,0"
                       CharacterSpacing="200"/>

            <TextBlock x:Uid="Loc_VerificationCode_Text"
                       FontSize="14"
                       Foreground="{ThemeResource AppMutedTextBrush}"
                       HorizontalAlignment="Center"
                       TextAlignment="Center"
                       Margin="0,4,0,0"/>

            <Button x:Uid="Loc_RefreshQRCode_Content" x:Name="RefreshButton"
                    Style="{StaticResource PillOutlineButtonStyle}"
                    Click="RefreshButton_OnClick"
                    HorizontalAlignment="Center"
                    Margin="0,20,0,0"/>
        </StackPanel>

        <!-- Tabbar at the bottom -->
        <local:Tabbar x:Name="tabbar" Grid.Row="2"/>
    </Grid>
</Page>
